# v2-only dependencies. Install on top of the legacy requirements.txt:
#   pip install -r requirements.txt -r requirements-v2.txt
#
# Pinned to versions known to work with v2's clean-architecture skeleton.
# Legacy app remains pinned to requirements.txt.

pydantic-settings==2.6.0
python-ulid==3.0.0

# OpenTelemetry — instrumentation is optional at runtime (configure_tracing
# is a no-op without an OTLP endpoint), but the SDK is required for the
# imports to succeed.
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-exporter-otlp-proto-http==1.27.0

# Quality gates
import-linter==2.1
ruff==0.6.9
mypy==1.11.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0
