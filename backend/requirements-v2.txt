# v2-only dependencies. Install on top of the legacy requirements.txt:
#   pip install -r requirements.txt -r requirements-v2.txt
#
# Pinned to versions known to work with v2's clean-architecture skeleton.
# Legacy app remains pinned to requirements.txt.

pydantic-settings==2.6.0
python-ulid==3.0.0

# OpenTelemetry — INSTALLED SEPARATELY in production via requirements-otel.txt
# (or your vendor's SDK). Excluded from requirements-v2.txt because every
# pinnable opentelemetry-api version caps importlib-metadata<8.8 and the
# legacy app pins importlib_metadata==9.0.0, making a unified resolver
# impossible. `shared/observability/tracing.py` is already wrapped in a
# try/import so the SDK is optional at runtime — without it, tracing is
# a no-op and the rest of the app boots fine.

# Quality gates
# `mypy` is already pinned by the legacy `requirements.txt` (==2.0.0).
# `pytest` is also pinned by legacy (==9.0.3); use a pytest-asyncio
# version that supports pytest 9 — 1.0+ does.
import-linter==2.1
ruff==0.6.9
pytest-asyncio==1.3.0
pytest-cov==5.0.0
